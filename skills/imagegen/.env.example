# imagegen 需要一个 OpenAI 兼容的「图像生成」API。
# 把本文件复制为 .env 并填入你自己的 key：
#   cp .env.example .env
#
# key 的解析顺序（见 SKILL.md「Setup」）：
#   1. 已经 export 到环境里的变量
#   2. 当前项目的 ./.env
#   3. ~/.claude/skills/imagegen/.env
#
# 切勿把填好真实 key 的 .env 提交到任何仓库（根 .gitignore 已默认忽略 .env）。

OPENAI_API_KEY=sk-replace-with-your-own-key

# 可选：自定义 API 端点。
# 用 OpenAI 官方端点时删掉下面这行即可；
# 用第三方中转 / 代理时填它的 /v1 地址（注意是否带端口）。
# OPENAI_BASE_URL=https://your-endpoint.example.com/v1
